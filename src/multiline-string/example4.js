var name = 'compañeros';
var message = `hola, ${name}.`;
console.log(message);

// var precio = 2000;
// var iva = 0.16;
// var message = `el valor del articulo es de ${precio}
// impuestos: ${precio*iva}
// total: ${(precio*iva) + precio}`;
// console.log(message);
